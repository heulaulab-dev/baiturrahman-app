# API Base URL
# Note: /v1 is appended automatically by the API client
# For local development:
NEXT_PUBLIC_API_URL=http://localhost:8080/api

# For production (update with your actual API URL):
# NEXT_PUBLIC_API_URL=https://api.your-domain.com/api

# Muslim Pro prayer-times proxy (server-only). Default uses Cloudflare Tunnel; set if you host elsewhere.
# MUSLIMPRO_SCRAPPER_BASE_URL=https://muslimpro-scrapper.lleans.dev
